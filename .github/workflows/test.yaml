name: Test
defaults:
  run:
    shell: bash
on:
  push:
    branches:
      - main
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Check validity
      run: python ./input_validity_test_main.py